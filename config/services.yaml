# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration
parameters:
    app.api_commerce: 'http://127.0.0.1:8000/soap?wsdl'
    bouchon.enabled: false
    bouchon.active_uc: 'WS_X_CHAMPS_VIDES'
    bouchon.directory: "unit_test/bouchons"
services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'
            - '../src/Tests/'
    
    # Définir le service CategorieService
    app.categorieService:
        class: App\Service\CategorieService
        arguments:
            $apikey: '%app.api_commerce%'

    App\Service\CategorieService: '@app.categorieService'
    # Définir le service ProduitService
    app.produitService:
        class: App\Service\ProduitService
        arguments:
            $apikey: '%app.api_commerce%'

    App\Service\ProduitService: '@app.produitService'


    # Définir le service CommerceAPI pour ProduitService
    commerce.produit:
        class: App\Service\CommerceAPI
        arguments:
            $ressourceInterface: '@app.produitService'

    # Définir le service CommerceAPI pour ProduitService 
    commerce.categorie:
        class: App\Service\CommerceAPI
        arguments:
            $ressourceInterface: '@app.categorieService'        

    App\Service\CommerceAPI: '@commerce.produit'
    App\Service\CommerceAPI $c: '@commerce.categorie'      

    all_data.commerceproduit:
        class: App\Service\AllData
        arguments:
            $service: '@commerce.produit'
            
    all_data.commercecategorie:
        class: App\Service\AllData
        arguments:
            - '@commerce.categorie'
    
    App\Service\AllData $commerceProduit: '@all_data.commerceproduit'
    App\Service\AllData $commerceCategorie: '@all_data.commercecategorie'

    # controllers are imported separately to make sure services can be injected
    # as action arguments even if you don't extend any base controller class
    App\Controller\:
        resource: '../src/Controller/'
        tags: ['controller.service_arguments']

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
